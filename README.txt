# 西九条店メニュー（8080対応・静的版）

このフォルダを8080のWebサーバー配下に置けば、`/nishikujo_menu_8080/` のようなパスで開けます。

- `index.html` が同じフォルダのExcelをブラウザで読み込みます。
- Excelを差し替えるだけでデータを更新できます。
- SheetJSをCDNから読み込むため、初回表示時はインターネット接続が必要です。
- GitHub Pagesでも同じ構成で動かせます。
